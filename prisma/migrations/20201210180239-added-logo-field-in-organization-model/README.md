# Migration `20201210180239-added-logo-field-in-organization-model`

This migration has been generated by alfieqashwa at 12/11/2020, 1:02:39 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "organizations" ADD COLUMN     "logo" TEXT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201209153330-creatednizations-one2many-to-users..20201210180239-added-logo-field-in-organization-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,26 +2,26 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id            Int       @id @default(autoincrement())
-  name          String?
-  email         String?   @unique
-  emailVerified DateTime? @map(name: "email_verified")
-  image         String?
-  role          Role      @default(USER)
-  createdAt     DateTime  @default(now()) @map(name: "created_at")
-  updatedAt     DateTime  @default(now()) @map(name: "updated_at")
-  posts         Post[]
-  organization  Organization? @relation(fields: [organizationId], references: [id])
+  id             Int           @id @default(autoincrement())
+  name           String?
+  email          String?       @unique
+  emailVerified  DateTime?     @map(name: "email_verified")
+  image          String?
+  role           Role          @default(USER)
+  createdAt      DateTime      @default(now()) @map(name: "created_at")
+  updatedAt      DateTime      @default(now()) @map(name: "updated_at")
+  posts          Post[]
+  organization   Organization? @relation(fields: [organizationId], references: [id])
   organizationId Int?
   @@map(name: "users")
 }
@@ -69,15 +69,16 @@
   @@map(name: "verification_requests")
 }
 model Organization {
-  id  Int @id @default(autoincrement())
-  email String? @unique
-  name String?
+  id      Int     @id @default(autoincrement())
+  email   String? @unique
+  name    String?
   address String?
-  phone String?
+  phone   String?
   website String?
-  users User[]
+  logo    String?
+  users   User[]
   @@map(name: "organizations")
 }
```


